# Claudio 开发日志

## 2026-06-26

### 全面审计修复 — 47项问题排查

**commit: 04819fe + 417b3f9 + b3151fb + a7afdb3**

分 P0/P1/P2 三个优先级对项目进行全面审计修复。

#### P0 后端修复（04819fe）

- 挂载 chatRouter / tasteRouter / planRouter 到 /api，修复 404 问题
- 添加 unhandledRejection / uncaughtException 全局处理，防进程崩溃
- recommend() fetch 添加 15s 超时，防下游服务无响应阻塞
- audio proxy 中 console.warn/error 替换为 logger，统一日志输出
- calendar.service / weather.service 添加关键操作日志

#### P0 前端修复（417b3f9）

- 统一收藏数据源：SearchPanel 改为从 App.tsx props 读取，移除独立 localStorage
- handleSelectSong 写入 claudio_recent_plays（最多 20 条）
- localStorage 同步移除 queue.length > 0 守卫，空队列也写入覆盖旧数据
- 删除 6 个死代码文件：ChatBar / Clock / Controls / DjMessage / useApi / api.ts
- 移除 useSocket.ts 中未使用的 useSocketEvent 导出

#### P1 修复（b3151fb）

- Socket.IO 重连逻辑修复
- CORS 策略收紧，限制允许的域名
- 错误信息中文化（common/errors.ts）
- Vite 生产构建关闭 sourcemap
- localStorage 读写优化

#### P2 修复（a7afdb3）

- wyy.json 同步读取改为异步 + mtime 缓存
- Admin 页面添加 error 状态（Dashboard / MusicLibrary / PlaylistManager）
- isAdmin 用 useRef 避免重复注册键盘监听
- BRAIN_API_URL 默认值修复为 base URL

---

## 2026-06-24

### 彻底修复媒体错误 code=4（音频代理 Range 支持）

**commit: 6f57d63**

**根因分析（4 Agent 并行诊断）：**

音频代理端点 `/api/audio-proxy` 声明 `Accept-Ranges: bytes` 但不处理客户端的 Range 请求。
当浏览器播放音频时，会发送 `Range: bytes=0-` 请求，代理返回 200 OK（完整文件）而非
206 Partial Content，浏览器丢弃响应 → 资源加载失败 → code=4。

**修复内容：**

| 层级 | 修复项 |
|------|--------|
| 音频代理 | Range 请求透传到上游，返回 206 + Content-Range |
| 音频代理 | 缓冲第一个 chunk 做 magic byte 验证后再写响应头 |
| 音频代理 | 添加 WAV (RIFF) magic byte 检测 |
| 音频代理 | Content-Type 拦截 text/html/text/plain 错误响应 |
| 音频代理 | 错误处理检查 headersSent 避免 "headers already sent" |
| 前端 play() | 快速路径移到 audio.src 赋值后，用 audio.currentSrc 比较 |
| 前端 play() | code=4 时自动调用 /api/song-url?force=true 重新获取 URL |
| 后端 API | getSongUrl 支持 force 参数绕过缓存 |
| 前端 onended | 添加 .catch() 防止 unhandled rejection |

---

### 彻底修复媒体错误 code=4（简化播放流程）

**commit: 86a2083**

移除 `removeAttribute('src')` + `audio.load()` 重置步骤，避免触发 MEDIA_ERR_ABORTED。
播放流程简化为：pause → 添加监听 → 设置 src → 等待 canplay → play()。

---

### 代码风险全面排查

**commit: 906467b + 871461e + 09bb3c4 + e00a439**

- Content-Type 去除 charset，octet-stream 按扩展名推断
- magic byte 验证（MP3/FLAC/OGG/M4A）
- SSRF 白名单支持子域名匹配
- MemoryCache 添加 10 分钟自动清理
- RateLimiter 添加 mutex 串行化并发调用
- 搜索/URL 失败返回 { success: false } 而非 mock 数据
- config.ts mock 默认值改为 false
- PlaylistService wyy.json 不存在时容错
- WeatherService API Key 缺失时回退 mock
- LOG_LEVEL 验证：无效值安全回退 info
- 移除 index.css 中重复的 fadeIn/pulse 定义
- SearchPanel 选择失败显示 toast 错误
- toggleLike 兼容 SongInfo[] 格式
- handleNext 使用 play() 而非 audio.play()

---

### 三档自适应字体 + 控制栏布局重构

**commit: 38b0254 + 034d744 + 9dd80e0 + 97af149**

- 控制栏三栏布局：左侧(queue+transport) | 中间(progress) | 右侧(utility)
- 队列按钮从右侧移到左侧（prev 按钮之前）
- 三档自适应字体：hiddenCount = (showCover?0:1) + (showTime?0:1)
  - 0: 14px（默认小字体）
  - 1: 18px（中等字体）
  - 2: 24px（大字体）
- 进度条左右 margin 20px 与控件保持距离

---

### 沉浸式歌词 + 音乐播放修复

**commit: 41fe879 + a3561b9 + 6eb088c**

- 沉浸式歌词重新设计
- 音量同步、错误提示、重试逻辑
- SSRF 白名单扩展
- QueueDrawer 播放状态同步（播放中显示动画条）
